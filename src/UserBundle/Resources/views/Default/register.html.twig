{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1 class="text-center">Please signup</h1>
        <div class="row">
            <div class="col-sm-4 col-sm-offset-4">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}